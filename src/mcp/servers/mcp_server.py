def run_server(host: str = "localhost", port: int = 3000):
    """Placeholder: HTTP/SSE server removed. Prints message and exits."""
    print(f"[MCP] HTTP/SSE server disabled. Requested start at http://{host}:{port}/ (noop).")

def main():
    run_server() 